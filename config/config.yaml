Mnist:
  val_size: 0.1
  dataset_output_dir: "../dataset/mnist"

Svhn:
  val_size: 0.1
  dataset_output_dir: "../dataset/svhn"

Mnist_M:
  val_size: 0.1
  dataset_output_dir: "../dataset/mnist_m"

protonet:
  n_way: 5
  k_shot: 5
  q_query: 15
  episodes: 100
  epochs: 200
  in_channels: 3
  hidden_dim: 64
  embedding_dim: 64
  lr: 0.001
  evalute_episodes: 1000

maml:
  n_way: 5
  k_shot: 1
  q_query: 15

  meta_batch_size: 1
  inner_updates: 2
  meta_iterations: 10000
  
  in_channels: 3
  hidden_dim: 64
  embedding_dim: 64
  
  alpha: 0.009 #inner lr
  beta: 0.0009 #meta lr

protomaml:
  n_way: 5
  k_shot: 1
  q_query: 15

  meta_batch_size: 1
  inner_updates: 1
  meta_iterations: 10000
  
  in_channels: 3
  hidden_dim: 64
  embedding_dim: 64
  
  alpha: 0.01 #inner lr
  beta: 0.001 #meta lr

dann_baseline:
  in_channels: 3
  hidden_dim: 64
  embedding_dim: 64

  batch_size: 512
  num_workers: 0
  lr: 0.001 
  epochs: 150

dann:
  in_channels: 3
  hidden_dim: 64
  embedding_dim: 64

  batch_size: 512
  num_workers: 0
  lr: 0.001
  epochs: 300

compare_experiments:
  n_way: 5
  ks: [1, 2, 4, 8, 16]
  q_query: 15
  episodes: 200

xtra:
  seed: 42
